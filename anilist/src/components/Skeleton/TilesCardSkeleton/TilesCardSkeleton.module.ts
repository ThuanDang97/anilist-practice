import { createStyles } from '@mantine/core'

export const useStylesTilesCardSkeleton = createStyles(() => ({
  productCard: {
    height: '80px',
    width: '80%',
    background: '#FFF',
    padding: '10px',
  },
}))
